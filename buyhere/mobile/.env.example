# URL de l'API BuyHere — la même que le site web (dossier server/, préfixe /api).
# Laisser vide en développement : l'app utilise automatiquement l'IP de votre
# PC détectée par Expo (http://<IP>:5000/api), ce qui fonctionne avec Expo Go.
# À renseigner pour un téléphone hors réseau local ou pour la production,
# par exemple : https://here-tn.onrender.com/api
EXPO_PUBLIC_API_URL=
